/* Voice widget — Marque Vendable (Poseidon)
 * La voix est en pause faute de crédits Vapi : le widget est intégré mais ne
 * s'affiche que lorsqu'un assistantId + publicKey Vapi seront renseignés.
 * Activation : remplir les deux champs ci-dessous (voir console Vapi).
 */
window.VOICE_CONFIG = {
  publicKey: "",       // ex: "abc123..." (clé publique Vapi)
  assistantId: "",     // ex: "a1b2c3d4-..." (assistant Vapi dédié au site)
  accent: "#22d3ee"
};
